import { buildTargets } from '../constants';

export class MainScene extends Phaser.Scene {
    constructor() {
        super({
            key: 'MainScene'
        });
    }

    init() {
        console.log('MainScene');
    }

    preload(): void {

    }

    create(): void {

    }
}
